<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ─── APPROVAL TEMPLATE ─── -->

    <record id="view_approval_template_form" model="ir.ui.view">
        <field name="name">approval.template.form</field>
        <field name="model">approval.template</field>
        <field name="arch" type="xml">
            <form string="Approval Template">
                <header>
                    <button name="action_view_requests" type="object" string="View Requests"
                            class="oe_stat_button" icon="fa-check-circle"
                            invisible="request_count == 0"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_requests" type="object"
                                class="oe_stat_button" icon="fa-check-circle">
                            <field name="request_count" widget="statinfo" string="Requests"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <label for="name" string="Template Name"/>
                        <h1><field name="name" placeholder="e.g. Purchase Approval > 10 000 €"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="model_id"/>
                            <field name="model_name" invisible="1"/>
                            <field name="amount_field"/>
                            <field name="currency_id"/>
                        </group>
                        <group>
                            <field name="escalation_days"/>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Approval Levels" name="levels">
                            <field name="level_ids" context="{'default_template_id': active_id}">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="name"/>
                                    <field name="approver_type"/>
                                    <field name="approver_user_id"
                                           invisible="approver_type != 'user'"
                                           required="approver_type == 'user'"/>
                                    <field name="approver_group_id"
                                           invisible="approver_type != 'group'"
                                           required="approver_type == 'group'"/>
                                    <field name="min_amount"/>
                                    <field name="max_amount"/>
                                    <field name="can_delegate"/>
                                    <field name="required"/>
                                    <field name="currency_id" invisible="1"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Description" name="description">
                            <field name="description"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_approval_template_list" model="ir.ui.view">
        <field name="name">approval.template.list</field>
        <field name="model">approval.template</field>
        <field name="arch" type="xml">
            <tree string="Approval Templates">
                <field name="name"/>
                <field name="model_id"/>
                <field name="escalation_days"/>
                <field name="request_count" string="Requests"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <record id="view_approval_template_search" model="ir.ui.view">
        <field name="name">approval.template.search</field>
        <field name="model">approval.template</field>
        <field name="arch" type="xml">
            <search string="Approval Templates">
                <field name="name"/>
                <field name="model_id"/>
                <filter string="Active" name="active" domain="[('active','=',True)]"/>
                <filter string="Archived" name="archived" domain="[('active','=',False)]"/>
            </search>
        </field>
    </record>

    <record id="action_approval_template" model="ir.actions.act_window">
        <field name="name">Approval Templates</field>
        <field name="res_model">approval.template</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first approval template
            </p>
            <p>Define multi-level approval matrices for purchase orders and expense reports.</p>
        </field>
    </record>

</odoo>
